if(${BUILD_TARGET} STREQUAL "X86")
  add_subdirectory(x86)
elseif(${BUILD_TARGET} STREQUAL "Hexagon")
  add_subdirectory(hexagon)
elseif(${BUILD_TARGET} STREQUAL "AArch64")
  add_subdirectory(aarch64)
endif()
